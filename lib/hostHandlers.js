'use strict';

var express = require('express');

var router = express.Router();

/**
 * This module should be replaced on host-specific branches to allow for custom tailored
 * endpoints and behaviors.
 */
module.exports = router;
